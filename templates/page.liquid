<div class="main-container" style="padding-top: 2rem; max-width: 800px; margin: 0 auto;">
    <h1 style="margin-bottom: 2rem;">{{ page.title }}</h1>
    <div style="background: var(--bg-card); padding: 2rem; border-radius: var(--radius-lg); box-shadow: var(--shadow-sm);">
        {{ page.content }}
    </div>
</div>
